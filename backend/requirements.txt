# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# DB
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pgvector==0.3.6

# Auth
pydantic==2.10.4
pydantic-settings==2.7.0
passlib[bcrypt]==1.7.4
argon2-cffi==23.1.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# Tasks
celery[redis]==5.4.0
redis==5.2.1

# Object storage
boto3==1.35.86

# Security / crypto for API keys (ADR-0006)
cryptography==44.0.0

# XML parsing (Open XML)
lxml==5.3.0

# Chinese tokenization (ADR-0004)
jieba==0.42.1

# HTTP client for model calls (OpenAI compatible)
httpx==0.28.1

# Image handling (vision scaling, ADR-0007) + perceptual hash for version match (ADR-0008)
Pillow==11.0.0
imagehash==4.3.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
